kind: PersistentVolume
apiVersion: v1
metadata:
  name: data2
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/tmp/test"
